<p id="notice"><%= notice %></p>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <form class="d-flex">
      <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success" type="submit">Search</button>
    </form>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-md-auto">
        <li class="nav-item">
          <%= link_to 'New Category', new_category_path , class: 'nav-item' %>
        </li>
      </ul>
    </div>
  </div>
</nav>
 <h1>Categories</h1>
      <div class="cntainer">
        <div class="d-flex row">
          <% @categories.each do |category| %>
          <div class="col-md-4">
            <div class="card mt-4">
              <div class="card-header">
                <td><%= category.category %></td>
                <td><%= link_to 'Show', category, class: "btn btn-warning" %></td>
                <td><%= link_to 'Edit', edit_category_path(category), class: "btn btn-danger" %></td>
                <td><%= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-secondary" %></td>
              </div>
              <div class="card-body">
              <%= link_to "View", courses_path(:cate => category.id), class: "btn btn-primary" %>
              <%= link_to image_tag("B.jpg", width: "200px") %>
              </div>
            </div>
          </div>
        <% end %>
        </div>
      </div>
